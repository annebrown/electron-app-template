{
    "name": "electron-app-template",
    "version": "1.0.0",
    "description": "Template for snippets and starter app",
    "main": "dist/main.js",
    "scripts": {
      "start": "pnpm exec tsc && electron .",
      "dev": "pnpm exec tsc -w & electron .",
      "build": "pnpm exec tsc",
      "package": "electron-packager . app --overwrite --platform=linux --arch=x64 --prune=true --out=release-builds --asar=true",
      "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "Anne Brown",
    "license": "ISC",
    "dependencies": {
      "electron": "^33.2.1"
    },
    "devDependencies": {
      "typescript": "^4.5.4"
    }
  }